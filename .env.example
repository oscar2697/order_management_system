# MySQL (docker-compose)
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=order_management
MYSQL_USER=app
MYSQL_PASSWORD=apppass
# Host port used to reach the Dockerized MySQL.
# 3307 by default because 3306 is often taken by a local MySQL install.
DB_PORT=3307
