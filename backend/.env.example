# Server
PORT=3001

# Database (when running OUTSIDE Docker, DB_HOST should be localhost)
DB_HOST=localhost
DB_PORT=3307
DB_USER=app
DB_PASSWORD=apppass
DB_NAME=order_management

# CORS origin (frontend)
CORS_ORIGIN=http://localhost:3000
